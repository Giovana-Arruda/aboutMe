import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

foto: {
    resizeMode: 'contain',
    borderRadius: 100,
    borderColor: '#78999f',
    borderWidth: 1,
    marginTop: 100,
}    

});